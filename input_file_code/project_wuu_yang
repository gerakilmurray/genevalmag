Algoritmo: project

function project (G, N) return a new graph
  /* G es un grafo. N es un conjunto de nodos de G. */
  
  GT := computo de la clausura transitiva de G
  
  Remover del grafo GT, todos los nodos que no pertenecen a N y todas las
     aristas que inciden en los nodos borrados.
     
  return GT
end function project
