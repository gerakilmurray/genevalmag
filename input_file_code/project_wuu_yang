Algoritmo: project

function project (G, N) retorna un grafo nuevo
  /* G es un grafo. N es un conjunto de nodos de G. */
  
  GT := computo de la clausura transitiva de G
  
  Remover del grafo GT, todos los nodos que no pertenecen a N y todas las aristas que incidian en ellos.
     
  return GT
end function project
